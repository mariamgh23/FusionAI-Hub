streamlit>=1.35.0
requests>=2.31.0
pandas>=2.0.0

# Document parsing (optional but recommended)
pypdf>=4.0.0
python-docx>=1.1.0

# Voice — speech-to-text
faster-whisper>=1.0.0

# Voice — mic capture
sounddevice>=0.4.6
soundfile>=0.12.1

# Voice — TTS (pick one or both)
pyttsx3>=2.90        # offline
gtts>=2.5.0          # online fallback

# Gmail integration (Task Agent)
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.126.0
